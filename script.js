var typed = new Typed(".auto-input", {
    strings: ["", "Seyer Rahimi"],
    typeSpeed: 100,
    backSpeed: 100,
    loop: true
  })
  
  
  function clearLocalStorage(){
      localStorage.clear();
  }
clearLocalStorage();